import { Textoa20pxDirective } from './textoa20px.directive';

describe('Textoa20pxDirective', () => {
  it('should create an instance', () => {
    const directive = new Textoa20pxDirective();
    expect(directive).toBeTruthy();
  });
});
