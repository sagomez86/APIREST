import { CreacionUsuarioPipe } from './creacion-usuario.pipe';

describe('CreacionUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new CreacionUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
